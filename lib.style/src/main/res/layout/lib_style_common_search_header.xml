<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/comm_topic_edit_text_layout"
    android:layout_width="match_parent"
    android:layout_height="46dp"
    android:paddingLeft="15dp"
    android:paddingRight="15dp"
    android:background="@color/lib_style_white"
    android:focusableInTouchMode="true">

    <EditText
        android:id="@+id/comm_search_edit_text"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:layout_centerVertical="true"
        android:background="@drawable/lib_style_corner_white_search_title"
        android:drawableLeft="@drawable/lib_style_comm_search"
        android:drawablePadding="2dp"
        android:focusable="false"
        android:clickable="true"
        android:hint=" 搜索"
        android:imeOptions="actionSearch"
        android:paddingLeft="10dp"
        android:textColor="#282828"
        android:textColorHint="#a0a0a0"
        android:textSize="14sp" />
</RelativeLayout>